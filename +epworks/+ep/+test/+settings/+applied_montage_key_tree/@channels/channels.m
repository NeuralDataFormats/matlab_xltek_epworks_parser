classdef channels < handle
    %
    %   ep.test.settings.applied_montage_key_tree.channels

    properties
        name
        full_name
        raw_data
        n_props
    end

    methods
        function obj = channels(s)
            %
            %   obj = ep.test.settings.applied_montage_key_tree.channels

        end
    end
end